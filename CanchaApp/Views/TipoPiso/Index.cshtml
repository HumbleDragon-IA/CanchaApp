@model IEnumerable<CanchaApp.Modelo.TipoPiso>

@{
    ViewData["Title"] = "Index";
}

<h4>Tipo Pisos</h4>

<p>
    <a asp-action="Create" class="btn btn-success">Crear nuevo Piso</a>
</p>
<div class="marco">
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoPiso1)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPiso1)
            </td>
          
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id " class="btn btn-primary">Editar Tipo de Piso</a> |
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-warning">Detalles</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Eliminar Tipo de Piso</a>
            </td>
        </tr>
}
    </tbody>
</table>
</div>